{
  "name": "indikatoren",
  "version": "1.0.0",
  "description": "Indikatorenportal Statistisches Amt Basel-Stadt",
  "dependencies": {
    "bootstrap": "3.3.7",
    "bootstrap-multiselect": "0.9.13-1",
    "highcharts": "5.0.7",
    "highcharts-export-csv": "1.4.4",
    "jquery": "3.1.1",
    "seedrandom": "2.4.2"
  },
  "devDependencies": {
    "gh-pages-travis": "1.0.0",
    "glob": "7.1.1",
    "highcharts-phantomjs": "2.2.0",
    "http-server": "0.9.0",
    "jsdom": "9.8.0",
    "rimraf": "2.5.4",
    "serialize-javascript": "^1.3.0",
    "shrinkpack": "0.18.1",
    "svgo": "0.7.2",
    "reinstall": "1.1.1",
    "copy-node-modules": "0.4.1"
  },
  "scripts": {
    "build:database": "node build/createJsonDatabase",
    "build:partial_databases": "node build/createKennzahlensetJsonFiles.js",
    "build:charts": "node build/createChartConfigs",
    "build:images_phantom_fast": "node build/createThumbnailsPhantomFast",
    "build:optimize_images": "echo 'Optimizing svg images...' && svgo --pretty --multipass -f images/portal/ && svgo --pretty --multipass -f images/indikatorenset/ && echo '...done optimizing images!'",
    "build:copy_modules": "echo 'Copying required node_modules to assets/js/modules...' && copy-node-modules . assets/js -v && rm -rf assets/js/modules && mv assets/js/node_modules assets/js/modules",
    "build": "npm run build:database && npm run build:partial_databases && npm run build:charts && npm run build:images_phantom_fast && npm run build:optimize_images && npm run build:copy_modules",
    "test": "echo \"No test specified\" && exit 0",
    "server": "http-server -p 8000 -c-1 -s -o ",
    "start": "npm run build && npm run server",
    "deploy:gh-pages": "gh-pages-travis",
    "deploy:ftp": "echo \"Fake uploading to FTP server...\" && exit 0",
    "deploy": "npm run deploy:gh-pages && npm run deploy:ftp && pwd",
    "reinstall": "reinstall",
    "build:update_dependencies": "reinstall && npm prune --production=false && npm update --dev",
    "build:shrinkpack": "npm shrinkwrap --dev && shrinkpack"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/statabs/indikatoren"
  },
  "author": "Jonas Bieri",
  "bugs": {
    "url": "https://github.com/statabs/indikatoren/issues"
  },
  "homepage": "http://www.statistik.bs.ch/zahlen/indikatoren/"
}
